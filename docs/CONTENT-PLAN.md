# KidPad 內容設計規劃書

對象：6 歲，台灣，幼小銜接階段。已會拿筆、認得部分國字、還沒學注音符號。
本文只談「要教什麼、怎麼教、練到什麼程度」，不含程式碼。

**已確認的前提（2026-09-06，家長回覆）**

| 項目 | 實際狀況 | 對設計的影響 |
|---|---|---|
| 算術程度 | **10 以內加減已經會了** | 算術直接從 Level 2 開始，Level 1 只留作降級後備 |
| 英文程度 | **26 個字母都認識** | 字母認讀不用分批慢慢開，重心移到發音與描寫 |
| 使用時間 | 不固定 | 維持一輪 6 題，隨時可以停，不做「每日任務」這種綁時間的設計 |
| 使用方式 | **大人在旁邊陪** | 題目可以保留文字說明，不必全部改成圖示＋語音 |
| 家長紀錄 | **要** | 增加一個家長頁（純本機、不上傳），見第 7 節 S5 |
| 名字在哪輸入 | **家長頁** | 大人設定一次就好，小朋友不用在練習畫面打字 |
| 練習時間怎麼算 | **每輪開始／結束累加** | 中途跑去玩別的會高估，接受這個誤差 |
| 星星上限 | **每 50 顆換獎章** | 見第 6 節 6-3 |
| 獎勵機制 | **要加動畫、闖關與收集要素** | 新增第 6 節整節：即時回饋動畫、關卡地圖、貼紙簿與獎章 |

---

## 1. 學習目標（每個模組一句話）

| 模組 | 目標 |
|---|---|
| 中文筆劃練習 | 讓小朋友能照正確筆順，把自己的名字和 60 個常用字寫出來 |
| 算術練習 | 讓小朋友看到 10 以內的加減式子，能不靠手指算出答案 |
| 英文練習 | 讓小朋友看到 26 個字母能認出來、唸得出來、寫得出來 |

---

## 2. 內容範圍與難度分級

### 2-1 中文筆劃練習

現在的做法是「輸入什麼字就練什麼字」，小朋友練了半天不知道自己在練什麼規則。建議補上前兩級。

**Level 1 — 基本筆劃（8 種）**

不用另外畫 SVG。Hanzi Writer 的資料庫裡這些單筆劃字元都查得到（已實測回 200），
直接當成「字」丟進現有的練習流程就會動：

| 筆劃 | 用這個字元練 | 小朋友聽得懂的說法 |
|---|---|---|
| 橫 | 一 | 從左邊畫到右邊 |
| 豎 | 丨 | 從上面畫到下面 |
| 撇 | 丿 | 從右上滑到左下 |
| 點 | 丶 | 輕輕點一下 |
| 捺 | 乀 | 從左上滑到右下 |
| 折 | 乙 | 轉一個彎 |
| 鉤 | 亅 | 到底了往上勾 |
| 提 | ㇀ | 從左下往右上挑 |

> 待驗證：「乀」「㇀」兩個字元的資料要在實作前先確認抓不抓得到，抓不到就改用
> 「人」（撇捺）、「打」的提來替代，或這兩個筆劃併到 Level 2 的字裡帶過。

畫面上每個筆劃要有名字（大字顯示「橫」），練的時候唸法用文字提示，不要只給動畫。

**Level 2 — 筆順規則（5 條）**

每條規則配 2–3 個示範字，先看動畫再自己寫，寫完跳出一句話講這條規則：

| 規則 | 示範字 |
|---|---|
| 從上到下 | 三、二 |
| 從左到右 | 川、州 |
| 先橫後豎 | 十、干、土 |
| 先撇後捺 | 人、八、大 |
| 先外後內 | 月、日、同 |

**Level 3 — 常用字與姓名**

分三組，每組是一個「關卡」，全部寫完一輪才解鎖下一組：

- **第一組 我的名字**（最優先）：家長第一次進來時輸入小朋友的姓名，這組永遠排在最前面。
- **第二組 筆劃 1–5 畫**：一 二 三 十 人 入 八 大 小 上 下 口 山 川 工 女 子 土 天 太 不 中 手 日 月 木 水 火 王 五 六 七 九 了 力 刀 又
- **第三組 生活常用字**：目 田 白 石 立 早 花 我 你 他 好 是 有 在 來 去 多 少 生 用 出 可 文 心 耳 目

姓名用字（陳林黃張李吳劉蔡楊許鄭謝郭洪邱曾廖賴明華志美玉英雅婷家宇軒恩安平晴柏翔宸睿薇晨語彤承祐廷宥辰品柔妤宜靜…）
已經打包在 `vendor/hanzi-data/`，離線可用；不在裡面的字第一次要連網抓。

---

### 2-2 算術練習

原本規劃是從數量開始，但小朋友 10 以內加減已經會了，所以**起點直接設在 Level 2**。
Level 1 不刪掉，留作「一輪錯太多題就降下去」的後備，也讓他偶爾拿個輕鬆的全對。

**Level 1 — 數與量（降級後備，不是起點）**

| 題型 | 長什麼樣 |
|---|---|
| 數一數 | 畫面出現 7 顆蘋果 → 從 3 個數字裡選出 7 |
| 數量對應 | 給數字 5 → 從 3 張圖裡選出剛好 5 個東西的那張 |
| 比大小 | 左邊 4 顆星、右邊 6 顆星 → 選「哪邊比較多」 |
| 接數字 | 3、4、□、6 → 選出中間少了哪個數字 |

**Level 2 — 10 以內加減（★ 起點，主力）**

| 題型 | 長什麼樣 |
|---|---|
| 圖像加法 | 🍎🍎🍎 ＋ 🍎🍎 = ? 圖案和算式一起出現，選項 3 選 1 |
| 圖像減法 | 5 顆餅乾吃掉 2 顆（被吃掉的畫成半透明）= ? |
| 純算式 | 3 ＋ 4 = ?（沒有圖，確認他不用數東西也算得出來） |
| 湊十 | 7 ＋ □ = 10，選出缺的那個數 |

**Level 3 — 20 以內（進位／退位）**

| 題型 | 長什麼樣 |
|---|---|
| 10 的分解 | 把 8 拆成 5 和 □ |
| 進位加法 | 8 ＋ 5 = ?，畫面用十格框（ten frame）示意先湊滿 10 |
| 退位減法 | 13 － 5 = ?，同樣用十格框 |

**輸入方式**：Level 1、2 一律 3 選 1 的大按鈕（6 歲打字很慢，鍵盤會毀掉節奏）。
Level 3 改成自繪的 0–9 大數字鍵盤，答案兩位數時才需要。

---

### 2-3 英文練習

順序仍然是「認 → 唸 → 寫」，但因為 26 個字母他都已經認得，
**Level 1 不分批、一次全開**，而且分量要壓縮 —— 它的任務從「教字母」變成「抓出還會混淆的那幾個」，
重心移到 Level 2 的發音和 Level 3 的描寫。

**Level 1 — 字母認讀（快速過關，不是主力）**

26 個字母一次全部進題庫，不分批。
一輪 6 題裡，**至少 4 題要來自易混淆組**（b/d、p/q、M/N/W、i/j、u/v），
其餘隨機。連續兩輪全對就自動解鎖 Level 2，不用一直重複練他早就會的東西。

| 題型 | 長什麼樣 |
|---|---|
| 大小寫配對 | 出現大寫 **B** → 從 b、d、p 三個小寫裡選 |
| 找字母 | 唸出 "C" → 從 3 個字母裡點出來 |
| 字母順序 | A、B、□、D → 選出缺的 |

刻意把 b/d、p/q、M/N 這些容易搞混的排在同一題當干擾選項 —— 這是這個年紀真正的難點。

**Level 2 — 字母發音（phonics 首音）**

| 題型 | 長什麼樣 |
|---|---|
| 聽音選字母 | 播放單字 "apple" → 選出開頭字母 A（唸整個字，不唸音素，理由見第 3 節） |
| 看圖選字母 | 出現蘋果圖 → 選 A |

字母 → 代表單字：A apple、B ball、C cat、D dog、E egg、F fish、G goat、H hat、I ice、
J jump、K kite、L lion、M moon、N nose、O orange、P pig、Q queen、R rabbit、S sun、
T tiger、U umbrella、V van、W water、X box、Y yoyo、Z zoo。

**Level 3 — 字母描寫**

四線三格底稿 + 淡灰字母，跟中文描紅同一種操作手感。
大寫先做（筆劃直、好判定），小寫延後。

---

## 3. 發音怎麼處理（已實測，結論如下）

**結論：用瀏覽器內建的 SpeechSynthesis，不放音檔。**

已在線上版實測：Windows Chrome 抓得到 3 個 en-US 語音（Microsoft Mark / Zira / David），
而且 `localService: true`，代表是系統內建語音，**離線也會出聲**。iPad 的 Safari 同樣有內建英文語音。

取捨說明：

| 做法 | 優點 | 缺點 | 採用 |
|---|---|---|---|
| SpeechSynthesis | 零檔案、離線可用、26 字母 + 26 單字全部免費 | 各家發音品質不一，機械感重，`/æ/` 這種音素唸不好 | ✅ |
| 預錄 mp3 | 發音準、可以用真人 | 要錄 50+ 個檔、repo 變大、要處理載入 | ❌ |
| 不做發音 | 最簡單 | 英文只剩認字形，學習效果砍半 | ❌ |

**因此 Level 2 的題型要改成「唸單字」而不是「唸音素」** —— 播 "apple" 整個字，
不要試著唸 /æ/，機器唸音素會失真到小朋友聽不懂。

保險做法：每次要發音前先檢查有沒有 en 語音，沒有的話那顆喇叭按鈕直接隱藏，
Level 2 自動退成「看圖選字母」，不要按了沒反應。

---

## 4. 畫面與互動設計

三個模組共用同一套骨架，小朋友換模組時不用重新學怎麼操作：

```
┌─────────────────────────────────┐
│  ⭐⭐⭐☆☆        第 3 題 / 共 6 題   │  ← 進度列，永遠在最上面
├─────────────────────────────────┤
│                                 │
│         題目區（最大）             │  ← 圖案／算式／字母，字級 60px 以上
│                                 │
├─────────────────────────────────┤
│   [  4  ]   [  5  ]   [  6  ]   │  ← 選項，每顆至少 120x80，手指點得到
└─────────────────────────────────┘
```

**答對**：選中的按鈕變綠、彈一下、`sound.good()` 上揚音，0.8 秒後自動下一題。
不要跳「答對了！」的對話框逼他按確認 —— 那會打斷節奏。

**答錯**：這是重點，6 歲挫折忍受度低。

- 錯的選項**淡出變灰**並輕輕左右晃一下，不變紅、不打叉、不出現「錯誤」兩個字
- `sound.hint()` 柔和的低音，不是警告音
- 提示文字用「再想想看～」「這個不是喔，再選一次」，不用「答錯了」
- 題目不會跳走，可以一直重選到對為止
- **不計時、不倒數、不扣分**。錯過的題目只在內部記下來，之後偷偷多出現幾次

**完成一輪**：沿用筆劃練習那個星星覆蓋層，換句台詞（「這一輪答對 6 題！」），
按鈕給兩個：再玩一次、換別的。

---

## 5. 出題與進度規則

**出題方式：固定題庫 + 亂數抽 + 簡易升降級。**

不用純亂數（會抽到一堆重複或難度亂跳），也不用複雜的自適應演算法（這個年紀不需要，
而且無法解釋為什麼突然變難）。規則就三條：

1. 每一級有自己的題庫，一輪從當前級抽題，不重複
2. 連續答對 3 題 → 下一輪升一級
3. 一輪內錯超過 2 題 → 下一輪降回上一級（**畫面上不要說「降級」**，安靜地換題就好）

**一輪幾題：6 題，約 3–5 分鐘。**
6 歲的專注力大約 10–15 分鐘，一輪 6 題讓他一次做 2–3 輪剛好，
而且答對率的分母小，容易拿到「全對」的成就感。

**進度呈現：沿用星星，三個模組一致。**

| 模組 | 一顆星怎麼拿 |
|---|---|
| 筆劃練習 | 完成一個字 = 1 顆星（現況，不改） |
| 算術練習 | 完成一輪 6 題 = 1 顆星，全對 = 2 顆 |
| 英文練習 | 同上 |

星星只累加、永遠不扣。左側選單的每個模組按鈕上顯示總星數，讓他自己看得到在累積。
星星累積到一定數量會換成獎章，一輪 6 題會包裝成關卡地圖上的「一關」—— 詳見第 6 節。

---

## 6. 成就感與獎勵設計

6 歲的學習動機幾乎全靠「立刻看得到的回饋」和「收集」。這一節把獎勵拆成三個層次：
**每一題**（0.5 秒）、**每一關**（3–5 分鐘）、**長期收集**（好幾週）。
全部用 CSS 動畫 + SVG + emoji 做得到，不需要美術素材，也不需要額外函式庫。

### 6-1 每一題：立即回饋

| 時機 | 畫面發生什麼 |
|---|---|
| 答對 | 按鈕彈一下變綠，**星星粒子從按鈕噴出來**（6–8 顆小星星向上散開後淡出，純 CSS transform） |
| 連續答對 2 題 | 畫面上方冒出「連對 2！」的小標籤，字會跳一下 |
| 連續答對 3 題以上 | 標籤換成火焰 🔥「連對 3！」，背景短暫閃一層暖色光 |
| 中文寫對一筆 | 該筆劃染色時加一道微光掃過（Hanzi Writer 畫完筆劃後疊一個 0.3 秒的高光） |
| 答錯 | 只有淡出變灰 + 輕晃，**沒有任何動畫獎勵，但也沒有懲罰動畫**（不掉血、不扣星） |

連對計數在答錯時歸零，但畫面不提示歸零這件事 —— 安靜地消失就好。

### 6-2 每一關：闖關地圖

把「一輪 6 題」包裝成「一關」，每個模組有一張橫向的關卡地圖：

```
  🏁 ─── ⭐ ─── ⭐ ─── 🔒 ─── 🔒 ─── 🎁
  第1關   第2關   第3關   第4關   第5關   寶箱
   ✔      ✔     ▶ 現在   還沒開   還沒開
```

- 一級（Level）＝一張地圖 ＝ 5 關，每關 6 題
- 過關的關卡點亮起並蓋上 ⭐；沒過的是灰色鎖頭
- **過關動畫**：小角色沿著路徑蹦跳前進到下一個點（0.8 秒），星星從天上掉下來
- 走到最後的 🎁 寶箱：打開來給一張貼紙（見 6-3），然後解鎖下一級的地圖
- 已經過的關可以重玩，重玩不再給貼紙但照樣給星星

進入模組時先看到這張地圖，不是直接跳進題目 —— 讓他自己選要打哪一關，
「還有幾關就開寶箱」這件事看得見，才會想再玩一輪。

### 6-3 長期收集：貼紙簿與獎章

**貼紙簿**（最強的長期誘因）

一面格子牆，未解鎖的貼紙是**灰色剪影**（看得到形狀、知道還缺什麼），解鎖時翻轉動畫變成彩色。

| 貼紙 | 怎麼拿到 |
|---|---|
| 🏅 名字達人 | 把自己的名字每個字都寫完一次 |
| ✏️ 筆劃小老師 | 8 個基本筆劃全部練完 |
| 🚀 心算火箭 | 算術連續答對 10 題 |
| 🔟 湊十高手 | 湊十題型全對一關 |
| 🔤 字母全能 | 26 個字母認讀全部答對過 |
| 🔊 小小播報員 | 發音題答對 20 題 |
| 🌈 三合一 | 同一天三個模組都玩過 |
| 🦉 早起鳥／🌙 夜貓子 | 早上 8 點前／晚上 8 點後練習過 |

貼紙只增不減，設計成 20 張左右，一開始就把全部格子顯示出來（灰色），
**看得到還缺 14 張**比看到已經有 6 張更有拉力。

**獎章**（把星星數變成看得懂的階段）

每 50 顆星換一個獎章，避免數字大到沒感覺：

| 星數 | 獎章 |
|---|---|
| 50 | 🥉 銅牌 |
| 100 | 🥈 銀牌 |
| 200 | 🥇 金牌 |
| 350 | 💎 鑽石 |
| 500 | 👑 皇冠 |

左側選單最上面顯示目前的獎章 + 距離下一個還差幾顆星（一條細細的進度條）。

**夥伴角色**（選配，S5 有餘力再做）

每個模組一隻用 emoji 或簡單 SVG 畫的小動物（筆劃 🐼、算術 🦊、英文 🐧），
待在題目區旁邊：答對時舉手跳一下，答錯時歪頭，一輪結束時比讚。
成本很低但陪伴感差很多 —— 尤其他自己一個人多玩幾輪的時候。

### 6-4 刻意不做的東西

| 不做 | 為什麼 |
|---|---|
| 排行榜、跟別人比較 | 只有一個使用者，而且這年紀比較只會帶來壓力 |
| 倒數計時、限時挑戰 | 使用時間本來就不固定，逼時間會讓他不想玩 |
| 體力值／連續登入天數 | 中斷一天就前功盡棄，反而變成負擔 |
| 會「輸」的機制（扣星、退關） | 已經拿到的東西不可以被收回，這是這個年紀的底線 |
| 抽獎、轉蛋 | 不想讓他把學習跟賭運氣連在一起 |

---

## 7. 開發階段規劃

已依家長回覆調整：原本的 S1（算術 Level 1 數與量）整個跳過，英文認讀壓縮成半個階段，
空出來的份量給家長頁。每一階段做完都能實際打開來給小朋友用。

| 階段 | 做什麼 | 為什麼排在這 |
|---|---|---|
| **S1** | 算術 Level 2（10 以內加減）＋ 題目骨架（進度列／選項按鈕／星星）＋ **6-1 的即時回饋動畫**（星星粒子、連對標籤） | 他的實際程度就在這一級，直接從能用的地方開始；即時回饋成本最低、效果最大，一開始就要有，骨架後面兩個模組直接沿用 |
| **S2** | 算術 Level 3（20 以內進退位、十格框）＋ 升降級規則＋ Level 1 後備題庫＋ **6-2 的關卡地圖與過關動畫** | 有了三級才有地圖可走，Level 1 這時候補上剛好當降級的底 |
| **S3** | 英文 Level 1（易混淆組快速過關）＋ Level 2（發音、看圖選字母）＋ **夥伴角色**（🐼🦊🐧） | 骨架已經有了，等於換題庫；字母都認得，所以 L1 和 L2 併成一個階段做完，順手把角色加進三個模組 |
| **S4** | 中文筆劃補強：8 個基本筆劃、5 條筆順規則、三組字表關卡（含「我的名字」）＋ 筆劃練習也接上地圖與動畫 | 現有模組升級而不是從零開始，可以拆著慢慢做，不擋其他模組 |
| **S5** | 家長頁（含名字設定）＋ **貼紙簿與獎章牆** ＋ 英文 Level 3（字母描寫） | 貼紙要等前面的成就條件都存在才有意義；家長頁也要等資料累積；描寫最難也最不急 |

### S5 的家長頁要放什麼（名字也在這裡設定）

純本機、讀 localStorage、不上傳、不需要密碼（但入口放在左側選單最下面，做成小按鈕，
不要讓小朋友誤點成主要功能）：

| 區塊 | 內容 |
|---|---|
| 這週練習 | 每天練了幾輪、總共幾分鐘（用開始／結束時間估） |
| 中文 | 每個字練過幾次、最近一次什麼時候；**哪些字重寫次數最多**（等於他老是寫錯的字） |
| 算術 | 目前在第幾級、答對率、**最常錯的題型**（加法／減法／湊十／進位） |
| 英文 | 目前在第幾級、**最常混淆的字母組**（b/d 之類） |
| 清除紀錄 | 放在最下面，要二次確認才會真的清掉 |

---

## 8. 驗收標準

用小朋友實際做得到的動作描述，不是技術檢查表。

**S1** — 小朋友點開算術練習，看到「🍎🍎🍎 ＋ 🍎🍎 = ?」和三個數字按鈕，點中 5 之後按鈕變綠、
**星星粒子噴出來**、有音效、自動跳下一題；連對第 2 題時上方冒出「連對 2！」；
做完 6 題跳出星星，左側「算術練習」按鈕上出現 ⭐。

**S2** — 小朋友點開算術練習先看到關卡地圖，第 1、2 關已經蓋上 ⭐，第 3 關是「現在」；
打完第 3 關，小角色蹦跳前進一格、星星從天上掉下來；走到第 5 關後寶箱打開。
連續答對 3 題後題目變難（8＋5 附十格框）；一輪錯 2 題以上安靜換回簡單題，畫面不出現「降級」字樣。

**S3** — 小朋友點喇叭聽到 "b"，從 b、d、p 裡選出 b；答對時旁邊的 🐧 舉手跳一下；
連續兩輪全對後自動進到看圖選字母。沒有英文語音的裝置上喇叭按鈕不會出現，按了不會沒反應。

**S4** — 小朋友先練完「橫、豎、撇、捺」四個基本筆劃拿到星星，
接著在「我的名字」關卡裡把自己的名字每個字都寫過一次；寫「十」的時候畫面會說「先橫後豎」。

**S5** — 小朋友打開貼紙簿，看到 20 個格子裡有 6 張彩色、14 張灰色剪影；
把名字全部寫完的那一刻，「🏅 名字達人」當場翻轉變成彩色。星星滿 50 顆時左上角出現 🥉。
大人點左下角進家長頁，看得到「這週練了 4 天」「『謝』重寫了 8 次」「減法答對率 60%」
「b/d 還會混」，並且可以在這裡設定小朋友的名字。
關掉平板隔天打開，星星、貼紙、獎章、紀錄都還在。

---

## 9. 已確認事項

兩輪問題家長都已回覆，結論都寫進文件了：

**第一輪** — 算術跳過 Level 1、英文認讀壓縮成快速過關、保留文字說明（有大人陪）、
加做家長頁、不綁每日任務。

**第二輪** — 名字在家長頁設定、練習時間用每輪起訖累加（接受高估）、每 50 顆星換獎章、
另外整個第 6 節的獎勵設計（即時動畫、關卡地圖、貼紙簿）都是這一輪加的。

目前沒有卡住的問題，可以直接從 S1 開工。

實作過程中會再冒出來、但現在不需要決定的事：貼紙總共要幾張（先抓 20 張，做的時候看情況調）、
夥伴角色要用 emoji 還是自己畫 SVG（先用 emoji，不夠可愛再說）。
